https://mohini.wisdmlabs.net/my-account/


Create plugins for the following requirements. 

1. Create a plugin to enable which allows the admin to create a content calendar for content publishing 
Currently, it should display a simple table
Day (date)
Occasion (e.g. Holi)
Post Title
Author -- (it should be one of the WordPress users)
Reviewer (it should be one of the WordPress users other than the author)
All rows will be taken as user input (form) in the plugin

Ideally, if you can reach better than this plugin, you will get bonus points 
